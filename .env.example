# Actual Budget Configuration

# Path to your Actual Budget data directory
# Default: ../actual-budget-cli/actual-data
ACTUAL_DATA_DIR=../actual-budget-cli/actual-data

# Your budget ID (found in Actual Budget)
# Example: My-Finances-42bc5bf
ACTUAL_BUDGET_ID=

# Actual Budget server URL
# Default: http://localhost:5006
ACTUAL_SERVER_URL=http://localhost:5006

# Actual Budget password for server authentication
ACTUAL_PASSWORD=

# Read-only mode: disable all write operations (set_*, create_*, update_*)
# Set to "true" or "1" to enable read-only mode
# Default: false (mutations allowed)
READ_ONLY=false
